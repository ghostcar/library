# Handoff — 2026-08-28

## Точка продолжения

- Рабочая ветка: `main`.
- Последний кодовый коммит до этого handoff: `b387f3f` (`feat(ui): show metadata and acquisition sources`).
- Развёрнутый кодовой baseline: `41a9068`.
- Рабочее дерево после этого handoff должно быть чистым.
- Коммиты `e857d7e..b387f3f` не deployed. Исходники в remote не push'ились; это требует отдельной команды владельца.

## Развёрнутый контур

- Test VPS: `ghcr.io/ghostcar/library:41a9068`.
- Alembic: `0010` (без изменений в UI rollout).
- Backup перед последним деплоем: `backups/pre-deploy/*-20260828-034009.*`, проверены gzip/tar.
- Web healthy, worker running без ложного HTTP healthcheck.
- Проверены `/healthz`, `/readyz`, no-auth redirect, CSP, локальный SVG sprite,
  внешний HTTPS и EPUBCheck 5.2.1.
- Rollback image: `ghcr.io/ghostcar/library:053c145`; БД откатывается только
  через backup/restore.

## Что уже сделано

- HTML error UX и redirect на login для browser routes.
- CSRF на unsafe SSR actions.
- Transactional outbox, recoverable jobs, async retention.
- Owner-scoped source uniqueness, streaming upload/scan, watched inbox.
- Responsive Ghostcar/Solar shell, local SVG icon sprite, strict CSP.
- Pinned browser toolchain: `playwright@1.62.1`, `npm ci`,
  `scripts/test-browser.sh`.
- После deployed baseline в коде: Flibusta OPDS metadata-only; migration 0011
  (`source_endpoints`, `source_links`); endpoint settings UI; author list/detail;
  direct source read-model и блоки у author/work.

## Следующие шаги по порядку

1. Ротировать JWT/AI/DB credentials, поскольку диагностическая команда
   `docker compose config` 2026-08-27 раскрыла значения `.env` в выводе. Ротация
   требует согласования владельца: JWT завершит текущие сессии, AI/DB требуют
   обновления внешних секретов и restart.
2. ~~Typed outbox handler registry + retry/backoff~~ РЕШЕНО в текущем срезе;
   внешние consumers и replay UI остаются будущей работой.
3. ~~Связать source observations с Work/Series и закрыть `has_new_release`,
   `last_observed`, `waiting_release` с evidence~~ РЕШЕНО в текущем срезе;
   ambiguous matches намеренно остаются без ссылки.
4. ~~Добавить formal adapter contracts/capabilities~~ Базовый контрактный слой
   реализован в ADR-0017; конкретные HTML/import/notification adapters — по мере надобности.
5. Завершить abstract source-management пакет (главная текущая задача):
   - сохранять `source_endpoint_id` в watch rule;
   - CRUD/toggle endpoint и SourceLink с owner validation;
   - preferred/priority отдельно для metadata/acquisition;
   - наследование `work > series > author > global`;
   - карточка серии и merged состояния «релиз известен / файл доступен»;
   - исправить author heading и добавить regression/integration tests.
6. Прогнать fresh migration 0011 (или 0011+следующую migration), full
   `scripts/test.sh`, browser smoke; только затем build/backup/migrate/deploy.
7. Выполнить дальнейшую визуальную сверку страниц по `design/`.
8. Исследовать Author.Today/Litnet HTML adapters после решений по auth,
   rate limits, selectors/fixtures и правовым ограничениям.

## Проверки

- `scripts/lint.sh` — clean.
- Последний полный `scripts/test.sh`: `263 passed` до abstract-source commits.
- После них: Ruff/diff-check clean на отдельных срезах; source tests 10 passed;
  frontend security 3 passed. Fresh migration test не стартовал из-за sandbox
  PermissionError при соединении с test PostgreSQL — это непроверенный gate, не test failure.
- `scripts/test-browser.sh` — desktop/mobile smoke passed.
- `git diff --check` — clean на момент handoff.
