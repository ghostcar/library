# TaskContext: visual series/source state

- Status: done
- Result: series list/detail now expose new-release, waiting-release, last-observed chips and a compact evidence disclosure.
- Scope deliberately excludes pixel snapshots and decorative artwork; the source-of-truth design keeps data legible and terminal-like.
